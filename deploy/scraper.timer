[Unit]
Description=PrecioTracker CR — Timer de scrape 2x/dia
Requires=precio-tracker-scraper.service

[Timer]
# 6:00 AM y 6:00 PM hora de Costa Rica (UTC-6 = 12:00 y 00:00 UTC)
OnCalendar=*-*-* 00,12:00:00 UTC
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
